#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @p: first number input
 * @q: second number input
 * Return: outcome
 */
int add(int p, int q)
{
	int r;

	r = p + q;
	return (r);
}
