#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints numbers between 0 to 99
 *
 * Return: 0 if no error is encountered 
 */
int main(void)
{
	int j, k, l, m, p1, p2;

	j = k = l = m = 48;
	while (j < 58)
	{
		k = 48;
		while (k < 58)
		{
			l=
